// I am using this file to define the types that I will use in my application
// Could be a good idea to use a folder for each section of the application
export interface Character {
  id: string;
  name: string;
}
